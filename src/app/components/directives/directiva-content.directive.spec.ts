import { DirectivaContentDirective } from './directiva-content.directive';

describe('DirectivaContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivaContentDirective();
    expect(directive).toBeTruthy();
  });
});
